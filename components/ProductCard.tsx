import Link from 'next/link';
import type { Product } from '@/lib/data';
export default function ProductCard({product}:{product:Product}){return <article className="product-card"><Link href={`/porcelain-tile/${product.slug}/`} className="card-media">{product.images.length?<img src={product.images[0]} alt={`${product.name} ${product.size} ${product.finish.toLowerCase()} porcelain tile`}/>:<div className="image-placeholder"><span>Product imagery coming soon</span></div>}<span className="price-badge">$1.95 / sq. ft.</span></Link><div className="card-body"><p className="eyebrow">{product.size} • {product.finish}</p><h3><Link href={`/porcelain-tile/${product.slug}/`}>{product.name}</Link></h3><div className="card-specs"><span>PEI 4</span><span>{product.edge}</span><span>{product.thickness}</span></div><div className="card-actions"><Link href={`/porcelain-tile/${product.slug}/`} className="text-link">View Product →</Link><Link href={`/request-quote/?product=${encodeURIComponent(product.name)}`} className="text-link">Request Quote</Link></div></div></article>}
