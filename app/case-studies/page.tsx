import type {Metadata} from 'next';import Link from 'next/link';import {cases} from '@/lib/data';
export const metadata:Metadata={title:'Porcelain Tile Design Applications & Case Studies',description:'Explore Falcor porcelain tile design applications, room visualizations and specification-focused project studies.'};
export default function Cases(){const published=cases.filter(c=>c.status==='complete');return <><section className="page-hero compact"><div className="shell"><p className="eyebrow">Design applications</p><h1>Porcelain in architectural context.</h1><p>Explore completed Falcor design studies built around verified product references, formats and finishes.</p></div></section><section className="section"><div className="shell"><div className="case-index-grid">{published.map(c=><Link href={`/case-studies/${c.slug}/`} className="study-card" key={c.slug}>{c.hero?<img src={c.hero} alt={`Design visualization featuring ${c.product}`}/>:<div className="study-placeholder">Visualization unavailable</div>}<div><p className="eyebrow">{c.product}</p><h2>{c.title}</h2><p>{c.challenge}</p><span>View study →</span></div></Link>)}</div></div></section></>}
