import type {Metadata} from 'next';import Link from 'next/link';import {notFound} from 'next/navigation';import {cases,products} from '@/lib/data';
export function generateStaticParams(){return cases.map(c=>({slug:c.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const c=cases.find(x=>x.slug===slug);return {title:c?`${c.title} — ${c.product} Design Application`:'Design Application',description:c?.challenge}}
export default async function Study({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const c=cases.find(x=>x.slug===slug);if(!c)return notFound();const p=products.find(x=>x.slug===c.productSlug)!;return <><section className="study-hero"><div className="shell"><div className="breadcrumbs"><Link href="/">Home</Link><span>/</span><Link href="/case-studies/">Case Studies</Link><span>/</span><span>{c.title}</span></div><div className="study-heading"><div><p className="eyebrow">{c.product} • Design application</p><h1>{c.title}</h1><p className="hero-lead">{c.challenge}</p></div><Link className="button button-light" href={`/porcelain-tile/${p.slug}/`}>View {p.name}</Link></div>{c.hero?<div className="study-image"><img src={c.hero} alt={`Architectural design visualization featuring ${c.product} porcelain tile`}/><span>Design visualization</span></div>:<div className="study-image study-placeholder large">Product visualization will be added when confirmed source photography is available.</div>}</div></section><section className="section"><div className="shell study-copy-grid"><div><p className="eyebrow">Design brief</p><h2>{c.challenge}</h2></div><div><h3>Proposed application</h3><p>{c.response}</p><h3>Design intent</h3><p>{c.intent}</p><h3>Project review</h3><p>{c.review}</p></div></div></section><section className="section stone"><div className="shell specs-layout"><div><p className="eyebrow">Featured material</p><h2>{p.title}</h2><p>Current Falcor price: <strong>$1.95 per sq. ft.</strong></p></div><dl className="spec-table"><div><dt>Size</dt><dd>{p.size}</dd></div><div><dt>Finish</dt><dd>{p.finish}</dd></div><div><dt>Thickness</dt><dd>{p.thickness}</dd></div><div><dt>Edge</dt><dd>{p.edge}</dd></div><div><dt>PEI</dt><dd>{p.pei}</dd></div><div><dt>Water Absorption</dt><dd>{p.absorption}</dd></div></dl></div></section><section className="section"><div className="shell narrow"><p className="visual-note">This page is an independent design application using Falcor product specifications and design visualization. It is not presented as a documented customer installation or endorsement.</p><div className="button-row"><Link className="button button-dark" href={`/porcelain-tile/${p.slug}/`}>View Product</Link><Link className="button button-light" href={`/request-quote/?product=${encodeURIComponent(p.name)}`}>Request Project Quote</Link></div></div></section></>}
